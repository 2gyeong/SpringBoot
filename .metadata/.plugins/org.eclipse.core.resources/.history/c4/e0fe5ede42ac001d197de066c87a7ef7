<html layout:decorate="~{layout}">
	<div layout:fragment="content" class = "container my-3">
	<table class = "table">
		<thead class = "table-dark"> <!--  제목을 처리 -->
	
		<tr>
			<td width="50">번호</td>
			<td width="250">이름</td>
			<td width="250">비밀번호</td>
			<td width="250">이메일</td>
			<td width="350">작성일</td>
			<td width="80">방문수</td>
		</tr>
		</thead>
	<tbody>
	<!--
		<tr th:each="question, loop : ${paging}">

			 <td th:text="${paging.getTotalElements -(paging.number*paging.size)-loop.index }"> 번호  </td>
			<td><a th:href="@{|users/detail/${users.name}|}">[[${users.name}]]</a></td>
			<td><a th:href="@{|users/detail/${users.pass}|}">[[${users.pass}]]</a></td>
			<td><a th:href="@{|users/detail/${users.email}|}">[[${users.email}]]</a></td>
			<td> [[ ${#temporals.format(users.regDate, 'yyyy-mm-dd HH:mm')}]] </td>
			<td><a th:href="@{|users/detail/${users.cnt}|}">[[${users.cnt}]]</a></td>
		</tr>
	-->
		
		<tr align="center" th:each="users : ${usersList}">
			<td>[[${users.idx}]]</td>
			<td><a th:href="@{|/users_detail/${users.idx}|}">[[${users.name}]]</a></td>
			<td>[[${users.pass}]]</td>
			<td>[[${users.email}]]</td>
			<td>[[${users.regDate}]]</td>
			<td>[[${users.cnt}]]</td>
		</tr>
	
		
	 	<!-- 페이징 
	 	<tr th:text="users, loop : ${paging}">
	 		<td th:text="${paging.getTotalElements - (paging.number*paging.size) - loop.index}"></td>
	 			<a th:href="@{|users/detail/${users.idx}|}">[[${users.name}]]</a>
		</tr>
		-->
		
	</tbody>
	</table>
		<a th:href="@{/user_insert}" class="btn btn-primary">회원 등록 </a>
</div>




</html>